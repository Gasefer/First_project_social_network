import preloader from '../../../Assets/Eclipse200px.svg';
import React from 'react';

let Preloader = (props) => {
    return <div>
        <img src={preloader} />
    </div>
}

export default Preloader;